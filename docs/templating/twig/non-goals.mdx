---
displayed_sidebar: templatingSidebar
title: Twig Non-Goals
sidebar_label: Non-Goals
sidebar_position: 3
description: Upstream PHP Twig features that @rhinostone/swig-twig throws on at parse time — sandbox, use, deprecated, macro kwargs, dynamic from, bracket-notation set. Explicit rather than silent. (Dynamic extends is supported on the async render path.)
level: intermediate
prereqs:
  - '[Overview](./)'
  - '[Parity](./parity)'
  - '[Security — the parser is the boundary](../swig/security#the-parser-is-the-boundary)'
---

# Twig Non-Goals

`@rhinostone/swig-twig` is a **strict subset** of upstream PHP Twig. Syntax outside the subset throws at parse time rather than silently producing a template that behaves differently from upstream. This page enumerates each rejection, the error message produced, and the rationale.

The rejections fall into three buckets:

1. **Sandbox / extension tags** that rely on runtime features swig-core does not expose.
2. **Deferred features** that will likely ship later but need dedicated IR design work.
3. **Intentional security rejections** where silent acceptance would weaken the CVE-2023-25345 guard perimeter.

## Unsupported tags

All of the following throw a generic `Unexpected tag "<name>"` error at parse time. They split into two categories:

- **Won't support (security).** Threat-model calls; unlikely to change.
- **Deferred.** Design work awaiting demand — [open an issue](https://github.com/gina-io/swig/issues) if you hit one.

### Won't support (security)

| Tag | Upstream purpose | Why rejected |
| --- | --- | --- |
| `{% sandbox %}` | Restrict the template runtime (whitelist tags, filters, functions). | swig-twig's threat model declares template source trusted (see [Security — the parser is the boundary](../swig/security#the-parser-is-the-boundary)). Inverting that requires closing runtime bracket access (`foo[varname]` where varname resolves to `"__proto__"` — a [known unclosable gap](../swig/security#what-the-guards-do-not-protect-against)), re-auditing every `.safe=true` filter as attacker-reachable, and accepting that `new Function(...)` compiles the attacker-controlled source. A partial sandbox that users trust is strictly worse than none. |
| `{% do %}` | Evaluate an expression for its side effects. | Every token-swallowing tag is a new `_dangerousProps` audit site, duplicated across frontend and backend (see [prototype-chain hardening](../swig/security#prototype-chain-hardening)). Small surface widening, for pure sugar over `{{ foo.method() }}` — autoescape already discards the return value. |

### Deferred

| Tag | Upstream purpose | Why not yet |
| --- | --- | --- |
| `{% embed %}` | Include with block-override. | Compound of `include` + block remapping. Most commonly requested of the three — likely to ship in a later minor if demand shows up. |
| `{% use %}` | Pull blocks in from a "trait" template without inheriting it. | Block composition is expressible via `extends` + `include` today. Needs a dedicated IR node. |
| `{% deprecated %}` | Emit a deprecation warning from within a template. | Trivial to build; low demand in swig-twig's likely audience. |

If you need one of these, either:

- Express the same intent with supported tags (e.g. `extends` + `block` for trait-style composition).
- Register a [custom tag](../swig/extending#custom-tags) scoped to your application.
- [Open an issue](https://github.com/gina-io/swig/issues) with a concrete use case — especially for the "Deferred" rows.

## Macro kwargs

Upstream Twig supports positional and keyword macro calls:

```twig
{% macro field(name, value=null, class="") %}…{% endmacro %}
{{ field("email", value=user.email, class="required") }}
```

swig-twig accepts the positional form — `{{ field("email", user.email, "required") }}` — but **rejects the keyword-argument form**. Lowering kwargs needs a per-macro parameter map in the IR, plus a runtime dispatcher in `swig-core`. Deferred — [open an issue](https://github.com/gina-io/swig/issues) with a concrete use case to prioritise.

Workarounds:

- Pass an object literal and destructure inside the macro:
  ```twig
  {% macro field(args) %}{{ args.name }} — {{ args.class ?? "" }}{% endmacro %}
  {{ field({ name: "email", class: "required" }) }}
  ```
- Order positional parameters from most-commonly-passed to least-commonly-passed so defaults "fall off the right."

## Dynamic `{% extends %}` {#dynamic-extends}

A dynamic parent — `{% extends parent_template %}` or `{% extends cond ? "a.twig" : "b.twig" %}` — **resolves on the async render path**: `renderFile(path, locals, cb)` against a loader that sets `loader.async === true`. The async-codegen path lowers the parent to a deferred expression and resolves it at render time through the loader. See [Async loaders](../swig/loaders#async-loaders).

| Shape | Sync render path | Async render path (`loader.async === true`) |
| --- | --- | --- |
| `{% extends "base.twig" %}` | Yes | Yes |
| `{% extends parent_template %}` | No — resolved at compile time | Yes |
| `{% extends cond ? "a.twig" : "b.twig" %}` | No — resolved at compile time | Yes |

On the **synchronous** render path the parent link is resolved at compile time, before any runtime value exists, so a dynamic parent surfaces a template-not-found / "no filename" error. Wrapping `{% extends %}` in `{% if %}` blocks does not rescue the sync path — whichever `extends` the parser sees last wins regardless of the runtime condition.

Sync-path workaround: **dispatch in the caller** and render the selected template directly.

```js
var template = (flavor === 'admin') ? 'page_admin.twig' : 'page_public.twig';
twig.renderFile(template, { /* locals */ });
```

## Dynamic `{% from %}`

`{% from %}` requires the path to be a **string literal** — a dynamic path throws `Dynamic "from" is not supported — path must be a string literal`:

| Shape | Accepted? | Error |
| --- | --- | --- |
| `{% from "macros.twig" import foo %}` | Yes | — |
| `{% from dynamic_path import foo %}` | No | `Dynamic "from" is not supported — path must be a string literal` |

`include` is not affected — its path can be any expression (and resolves dynamically on the async render path too).

## Bracket-notation `set`

Dot-path LHS is supported; bracket-notation LHS throws:

| Shape | Accepted? | Error |
| --- | --- | --- |
| `{% set foo = 1 %}` | Yes | — |
| `{% set foo.bar = 1 %}` | Yes | — |
| `{% set foo["bar"] = 1 %}` | No | `Bracket-notation assignment is not supported in "set" (use dot-path notation)` |
| `{% set foo[dynamic_key] = 1 %}` | No | Same as above. |

Rationale: bracket-notation writes at runtime can reach through the prototype chain (`foo["__proto__"]["x"] = …`) in ways that the parse-time `_dangerousProps` check cannot fully enclose. Dot-path writes are statically analysable — each segment is a lexer-validated identifier — and the native swig frontend already went through this audit during [prototype-chain hardening](../swig/security#prototype-chain-hardening). swig-twig inherits the same perimeter.

## Conditional branches (deferred)

Upstream Twig supports multi-branch conditionals — `{% if %} … {% elseif %} … {% else %} … {% endif %}` — and an empty-fallback form on `{% for %}` — `{% for %} … {% else %} … {% endfor %}` (runs when the iterable is empty). swig-twig currently ships only the single-branch form.

| Shape | Accepted? | Error |
| --- | --- | --- |
| `{% if x %}…{% endif %}` | Yes | — |
| `{% if x %}…{% else %}…{% endif %}` | No | `Unexpected tag "else"` |
| `{% if x %}…{% elseif y %}…{% endif %}` | No | `Unexpected tag "elseif"` |
| `{% for x in items %}…{% endfor %}` | Yes | — |
| `{% for x in items %}…{% else %}…{% endfor %}` | No | `Unexpected tag "else"` |

Rationale: the single-branch shape shipped first to unblock the rest of the Twig parser surface. Multi-branch lowering needs a dedicated branch-chain IR node plus the frontend handler that consumes `else` / `elseif` tokens without pushing them on the open-tag stack. [Open an issue](https://github.com/gina-io/swig/issues) with a concrete use case to prioritise.

Available workarounds:

- **`{% if %} / {% else %}` → two `if` blocks with `not`:**
  ```twig
  {% if x %}yes{% endif %}
  {% if not x %}no{% endif %}
  ```
- **`{% if %} / {% elseif %} / {% else %}` → nested `if`:**
  ```twig
  {% if x %}a{% endif %}
  {% if not x %}
    {% if y %}b{% endif %}
    {% if not y %}c{% endif %}
  {% endif %}
  ```
  Uglier than the native form but semantically equivalent.
- **`{% for … %}{% else %}…{% endfor %}` → guard with `{% if items|length %}` before the loop:**
  ```twig
  {% if items|length %}
    {% for item in items %}{{ item }}{% endfor %}
  {% endif %}
  {% if not items|length %}empty{% endif %}
  ```

## Other known gaps

These are tracked but not yet implemented:

- **`date_modify` filter.** Relies on DateTime-object mutation; deferred.
- **Locale-aware `date` / `number_format`.** swig-twig's `date` uses the shared PHP-style formatter from `@rhinostone/swig-core`; locale rules (weekday names, currency formats) are not plumbed through yet.
- **Runtime bracket-access guard.** `{{ foo[varname] }}` with `varname` resolving to `"__proto__"` at runtime is not blocked. Documented at the engine level — see [Security — what the guards do NOT protect against](../swig/security#what-the-guards-do-not-protect-against).

## Reporting a rejection that feels wrong

If the rejection you hit isn't listed here, please [open an issue on `gina-io/swig`](https://github.com/gina-io/swig/issues) with a minimal reproducer — we track rejections against the design-doc subset and may be able to promote a gap to a supported feature.
